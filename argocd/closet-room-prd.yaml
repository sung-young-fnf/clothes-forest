apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: closet-room-prd
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: closet-room
  source:
    repoURL: https://github.com/fnf-process/clothes-forest.git
    targetRevision: main
    path: charts/closet-room
    helm:
      valueFiles:
        - values.yaml
        - values-prd.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: closet-room-prd
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 5m
